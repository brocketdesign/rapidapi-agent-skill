# Copy to .env for local dev (.env is gitignored). Do NOT commit real secrets.
PORT=3000
NODE_ENV=development
# Set this in your hosting dashboard AFTER creating the RapidAPI listing:
RAPIDAPI_PROXY_SECRET=
# Any upstream API key your service wraps:
EXTERNAL_API_KEY=
